# CodeSense知识点画像系统 - 部署指南

## 已完成的核心功能

### 1. 数据库结构 ✅
- **knowledge_point_scores** - 学生知识点评分表
- **assignment_knowledge_points** - 作业知识点关联表

**SQL创建脚本**: `migrations/add_knowledge_point_system.sql`

### 2. 数据模型 ✅
- `KnowledgePointScore` - 知识点评分模型（models.py:625-750）
  - 方法: `get_or_create()`, `update_score()`, `get_student_profile()`
  - 支持13个C语言知识点
- `AssignmentKnowledgePoint` - 作业知识点模型（models.py:753-807）
  - 方法: `add_to_assignment()`, `get_assignment_knowledge_points()`

### 3. AI评估器扩展 ✅
- **流式分析**: `analyze_ability_trend_stream()` (ai_evaluator.py:495-609)
  - 使用SSE实时推送分析结果
  - 替代原有的异步队列机制

- **知识点检测**: `detect_code_knowledge_points()` (ai_evaluator.py:611-701)
  - AI自动检测代码涉及的知识点
  - 回退方案：基于标题关键词推断

### 4. 流式API端点 ✅
- **GET /api/stream/ability-analysis** (api.py:908-1019)
  - SSE格式实时推送分析进度
  - 返回知识点画像数据
  - 流式输出AI分析文本

### 5. 提交时自动更新知识点 ✅
- **位置**: assignments.py:432-487
- **逻辑**:
  1. 检查作业是否有教师标注的知识点
  2. 如有，直接使用标注更新评分
  3. 如无，调用AI自动检测并保存
  4. 更新学生的知识点评分记录

---

## 部署步骤

### 第一步：创建数据库表

```bash
# 方法1：使用MySQL客户端
mysql -u your_username -p your_database < migrations/add_knowledge_point_system.sql

# 方法2：使用phpMyAdmin
# 打开phpMyAdmin，选择数据库，导入SQL文件
```

### 第二步：重启Flask应用

```bash
cd E:\CodeSense\源代码
python app.py
```

### 第三步：测试流式API

打开浏览器开发者工具，访问：
```
http://localhost:5000/api/stream/ability-analysis
```

应该看到SSE事件流输出。

---

## 前端集成（待实现）

### 需要修改的文件
1. **student_home.html** - 学生首页
   - 移除旧的轮询机制
   - 添加SSE连接代码
   - 添加知识点雷达图

2. **前端JavaScript示例**:
```javascript
const eventSource = new EventSource('/api/stream/ability-analysis');

eventSource.onmessage = function(event) {
    const data = JSON.parse(event.data);

    if (data.type === 'progress') {
        updateProgress(data.percent, data.message);
    } else if (data.type === 'knowledge_profile') {
        updateKnowledgeRadar(data.data);
    } else if (data.type === 'analysis_chunk') {
        appendAnalysisText(data.content);
    } else if (data.type === 'complete') {
        eventSource.close();
    }
};
```

---

## 工作原理

### 流程图
```
学生提交代码
    ↓
[评估代码] → 计算得分
    ↓
[检查作业知识点]
    ├─ 有教师标注 → 直接使用
    └─ 无标注 → AI检测 → 保存到数据库
    ↓
[更新学生知识点评分]
    ├─ 考虑难度系数
    ├─ 考虑权重
    └─ 加权平均计算新分数
    ↓
[学生查看首页]
    ↓
[SSE连接] → 实时推送
    ├─ 知识点画像数据
    ├─ AI分析进度
    └─ 流式文本输出
```

### 知识点评分算法

```python
# 公式：新分数 = 0.6 * 旧分数 + 0.4 * (本次得分 * 难度系数 * 权重)
weighted_score = assignment_score * difficulty * weight
new_score = 0.6 * old_score + 0.4 * weighted_score

# 难度系数范围：0.5 - 2.0
# 权重范围：0.1 - 2.0
# 最终分数：0 - 100
```

---

## 支持的C语言知识点

1. **basic_syntax** - 基础语法
2. **pointer** - 指针
3. **function** - 函数
4. **array** - 数组
5. **string** - 字符串
6. **struct** - 结构体
7. **file_io** - 文件操作
8. **dynamic_memory** - 动态内存
9. **linked_list** - 链表
10. **tree** - 树
11. **sorting** - 排序算法
12. **searching** - 搜索算法
13. **recursion** - 递归

---

## 下一步待实现功能

1. ✅ 流式SSE API
2. ✅ 知识点自动更新
3. ⏳ 前端SSE集成
4. ⏳ 知识点雷达图可视化
5. ⏳ 教师标注界面
6. ⏳ 学生知识点详情页面

---

## 测试建议

1. **提交一个关于指针的作业**
   - 检查是否自动检测到"pointer"知识点
   - 查看数据库`knowledge_point_scores`表

2. **多次提交同一题目**
   - 验证分数是否按加权平均更新

3. **访问流式API**
   - 观察SSE事件是否正常推送
   - 检查AI分析是否实时显示

---

## 故障排查

### 问题1：知识点未更新
- 检查日志：`current_app.logger`输出
- 确认AI API key已配置
- 查看数据库表是否创建成功

### 问题2：SSE连接失败
- 检查浏览器是否支持EventSource
- 确认Flask版本支持`stream_with_context`
- 查看网络面板的SSE连接状态

### 问题3：AI检测不准确
- 调整prompt模板
- 增加关键词映射规则
- 考虑教师手动标注
