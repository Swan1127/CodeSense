# 知识点画像系统 - 后端功能测试指南

## 测试前准备

### 1. 创建数据库表

**方法一：使用MySQL命令行**
```bash
# 进入MySQL
mysql -u root -p

# 选择数据库（替换成你的数据库名）
USE your_database_name;

# 执行SQL文件
source E:/CodeSense/源代码/migrations/add_knowledge_point_system.sql;

# 验证表是否创建成功
SHOW TABLES LIKE '%knowledge%';
```

**方法二：直接执行SQL**
```sql
-- 复制 migrations/add_knowledge_point_system.sql 中的内容
-- 粘贴到MySQL客户端或phpMyAdmin执行
```

### 2. 确认环境变量

检查 `.env` 文件中是否配置了：
```bash
ZHIPU_API_KEY=your_api_key_here
```

### 3. 重启Flask应用

```bash
cd E:\CodeSense\源代码
python app.py
```

---

## 测试流程

### 测试1：数据库表验证

**目标**：确认新表已创建

```sql
-- 查看表结构
DESCRIBE knowledge_point_scores;
DESCRIBE assignment_knowledge_points;

-- 预期结果：
-- knowledge_point_scores 应有字段：
--   id, student_id, knowledge_point, score, total_attempts,
--   correct_attempts, average_difficulty, last_updated, created_at

-- assignment_knowledge_points 应有字段：
--   id, assignment_id, knowledge_point, weight, difficulty,
--   auto_detected, created_at
```

**通过标准**：两个表都存在且字段完整

---

### 测试2：提交代码测试知识点自动检测

**目标**：验证提交代码时自动检测和更新知识点

**步骤**：

1. **登录系统**
   - 使用学生账号登录

2. **提交一个包含指针的代码**
   ```bash
   # 访问任意作业的提交页面
   # 提交以下测试代码：
   ```

   ```c
   #include <stdio.h>
   #include <stdlib.h>

   int main() {
       int *ptr = (int*)malloc(sizeof(int));
       *ptr = 42;
       printf("Value: %d\n", *ptr);
       free(ptr);
       return 0;
   }
   ```

3. **查看日志输出**
   ```bash
   # 在终端查看Flask日志，应该看到：
   # "作业 X 无知识点标签，使用AI自动检测"
   # "AI检测到 N 个知识点并已更新评分"
   ```

4. **检查数据库**
   ```sql
   -- 查看你的知识点评分
   SELECT * FROM knowledge_point_scores
   WHERE student_id = 'YOUR_STUDENT_ID';

   -- 预期结果：应该看到 pointer, dynamic_memory 等知识点记录

   -- 查看作业的知识点标签
   SELECT * FROM assignment_knowledge_points
   WHERE assignment_id = YOUR_ASSIGNMENT_ID;

   -- 预期结果：应该看到自动检测的知识点（auto_detected = 1）
   ```

**通过标准**：
- 日志显示AI检测成功
- knowledge_point_scores 表有新记录
- assignment_knowledge_points 表有新记录
- auto_detected 字段为 TRUE

---

### 测试3：流式API测试

**目标**：验证SSE流式输出是否工作

**方法一：使用浏览器（推荐）**

1. **登录系统**
   - 先在浏览器登录（建立session）

2. **打开开发者工具**
   - 按 F12
   - 切换到 "网络" (Network) 标签

3. **访问流式API**
   - 新标签页访问：`http://localhost:5000/api/stream/ability-analysis`
   - 或直接在控制台执行：
   ```javascript
   const eventSource = new EventSource('/api/stream/ability-analysis');
   eventSource.onmessage = (e) => console.log(JSON.parse(e.data));
   eventSource.onerror = (e) => console.error('Error:', e);
   ```

4. **观察输出**
   - 应该看到实时的JSON数据流
   - 类型包括：progress, knowledge_profile, analysis_chunk, complete

**方法二：使用curl**

```bash
# 首先获取登录cookie
curl -c cookies.txt -X POST http://localhost:5000/auth/login \
  -d "username=YOUR_USERNAME&password=YOUR_PASSWORD"

# 使用cookie访问流式API
curl -b cookies.txt -N http://localhost:5000/api/stream/ability-analysis

# 预期输出：
# data: {"type":"progress","percent":10,"message":"正在加载知识点数据..."}
# data: {"type":"knowledge_profile","data":{...}}
# data: {"type":"analysis_chunk","content":"分析文本..."}
# ...
```

**方法三：使用Python脚本**

创建测试脚本 `test_stream.py`：
```python
import requests
import json

# 1. 登录获取session
session = requests.Session()
login_data = {
    'username': 'YOUR_USERNAME',
    'password': 'YOUR_PASSWORD'
}
login_response = session.post('http://localhost:5000/auth/login', data=login_data)
print(f"登录状态: {login_response.status_code}")

# 2. 访问流式API
response = session.get('http://localhost:5000/api/stream/ability-analysis', stream=True)

print("\n开始接收SSE流：")
print("="*50)

for line in response.iter_lines():
    if line:
        line_str = line.decode('utf-8')
        if line_str.startswith('data: '):
            data = json.loads(line_str[6:])
            print(f"\n[{data['type']}]", end=" ")

            if data['type'] == 'progress':
                print(f"{data['percent']}% - {data['message']}")
            elif data['type'] == 'knowledge_profile':
                print(f"收到知识点数据，共 {len(data['data'])} 个知识点")
            elif data['type'] == 'analysis_chunk':
                print(data['content'], end='')
            elif data['type'] == 'complete':
                print("\n分析完成！")
                break

print("\n" + "="*50)
```

运行：
```bash
python test_stream.py
```

**通过标准**：
- 能够建立SSE连接
- 收到 knowledge_profile 数据
- 收到流式的 analysis_chunk
- 最后收到 complete 消息

---

### 测试4：知识点评分更新测试

**目标**：验证多次提交后分数按加权平均更新

**步骤**：

1. **第一次提交**（高分代码）
   ```c
   // 提交一个完整的指针代码，得分 4-5 分
   int *arr = (int*)malloc(10 * sizeof(int));
   for(int i = 0; i < 10; i++) arr[i] = i;
   free(arr);
   ```

2. **查看初始分数**
   ```sql
   SELECT knowledge_point, score, total_attempts
   FROM knowledge_point_scores
   WHERE student_id = 'YOUR_ID' AND knowledge_point = 'pointer';
   -- 记录分数，例如：85分
   ```

3. **第二次提交**（低分代码）
   ```c
   // 提交一个有问题的代码，得分 1-2 分
   int *p;
   *p = 10;  // 野指针
   ```

4. **查看更新后分数**
   ```sql
   SELECT knowledge_point, score, total_attempts
   FROM knowledge_point_scores
   WHERE student_id = 'YOUR_ID' AND knowledge_point = 'pointer';
   -- 分数应该降低，但不会完全变成新分数
   -- 因为使用了加权平均：0.6 * 旧分数 + 0.4 * 新分数
   ```

**通过标准**：
- total_attempts 正确增加
- 分数按加权平均公式更新
- 不是简单的覆盖

---

### 测试5：知识点画像数据完整性测试

**目标**：验证 get_student_profile 方法返回完整数据

**方法：使用Flask Shell**

```bash
# 进入Flask Shell
cd E:\CodeSense\源代码
python
```

```python
>>> from app import app, db
>>> from models import KnowledgePointScore
>>>
>>> with app.app_context():
>>>     profile = KnowledgePointScore.get_student_profile('YOUR_STUDENT_ID')
>>>     import json
>>>     print(json.dumps(profile, indent=2, ensure_ascii=False))

# 预期输出：
# {
#   "pointer": {
#     "score": 85.0,
#     "name": "指针",
#     "total_attempts": 2,
#     "correct_attempts": 1,
#     "accuracy": 50.0,
#     "average_difficulty": 1.0,
#     "last_updated": "2025-01-17 10:30"
#   },
#   "basic_syntax": {
#     "score": 0,
#     "name": "基础语法",
#     "total_attempts": 0,
#     ...
#   },
#   ... (其他11个知识点)
# }
```

**通过标准**：
- 返回13个知识点的完整数据
- 已测试的知识点有分数
- 未测试的知识点为0

---

## 常见问题排查

### 问题1：表创建失败

**症状**：执行SQL报错 "Table already exists" 或语法错误

**解决**：
```sql
-- 先删除旧表（如果存在）
DROP TABLE IF EXISTS assignment_knowledge_points;
DROP TABLE IF EXISTS knowledge_point_scores;

-- 重新执行创建SQL
```

### 问题2：知识点未自动检测

**症状**：提交代码后 knowledge_point_scores 表无新记录

**排查步骤**：

1. **检查日志**
   ```bash
   # 查看终端日志，搜索关键词
   # "知识点" 或 "knowledge"
   ```

2. **检查AI API配置**
   ```python
   # 在Flask Shell中
   >>> import os
   >>> print(os.environ.get('ZHIPU_API_KEY'))
   # 应该输出你的API key
   ```

3. **手动测试AI检测**
   ```python
   >>> from services.ai_evaluator import AIEvaluator
   >>> api_key = os.environ.get('ZHIPU_API_KEY')
   >>> evaluator = AIEvaluator(api_key)
   >>> result = evaluator.detect_code_knowledge_points(
   ...     "int *ptr = malloc(sizeof(int));",
   ...     "指针测试"
   ... )
   >>> print(result)
   # 应该返回包含 'pointer' 的列表
   ```

### 问题3：SSE连接失败

**症状**：访问流式API返回404或403

**解决**：

1. **确认已登录**
   ```bash
   # SSE需要登录session
   # 先在浏览器登录，再访问API
   ```

2. **检查路由注册**
   ```python
   # 在Flask Shell中
   >>> from app import app
   >>> print([r for r in app.url_map.iter_rules() if 'stream' in r.rule])
   # 应该看到 /api/stream/ability-analysis
   ```

3. **测试简单的SSE**
   ```python
   # 创建测试路由 test_sse.py
   from flask import Response
   import json
   import time

   def test_sse():
       def generate():
           for i in range(5):
               yield f"data: {json.dumps({'count': i})}\n\n"
               time.sleep(1)
       return Response(generate(), mimetype='text/event-stream')
   ```

---

## 成功标准总结

完成以上所有测试后，你应该能够：

- ✅ 数据库中有两个新表
- ✅ 提交代码后自动检测知识点
- ✅ knowledge_point_scores 表有评分记录
- ✅ SSE流式API返回实时数据
- ✅ 知识点画像数据完整（13个知识点）
- ✅ 多次提交后分数按加权平均更新

---

## 下一步

后端测试通过后，可以开始前端集成：

1. 修改 student_home.html
2. 添加SSE连接代码
3. 实现知识点雷达图
4. 流式显示AI分析文本

前端集成指南：见 `知识点画像系统部署指南.md`
