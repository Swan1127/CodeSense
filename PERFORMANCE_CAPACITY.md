# CodeSense 性能与容量评估

评估日期：2026-09-02

这是一份面向当前部署规格的工程估算，不是压测承诺。最终上线容量还会受
AI 供应商配额、数据库磁盘性能、C++ 编译任务比例、页面大小和实际访问曲线
影响；上线前应按本文的验证方法做一次带真实数据的压测。

## 结论先行

截图中的服务器为：2 vCPU、2 GiB 内存、3 Mbps 公网带宽、40 GiB ESSD Entry
系统盘。它适合开发、演示和一个小规模课堂试用，但不适合作为未来高并发的
唯一生产节点。

在当前代码和保守运行参数下，可以先按“约 10–20 名活跃用户、AI 请求不持续
同时爆发”的小规模课堂估算。这里的活跃用户指最近 10–30 秒内正在操作页面，
不是系统中的账号总数。若一整个班级在同一时刻集中提交代码或同时触发 AI，
瓶颈会首先出现在 AI 并发、CPU 编译和 3 Mbps 带宽，而不是账号数量。

建议的升级顺序是：先把公网带宽提高到至少 10 Mbps，再将应用节点升级到
4 vCPU / 8 GiB；数据库和 Redis 达到多人、多班级使用规模后应独立部署或使用
托管服务。若目标超过 100 名活跃用户，建议至少 8 vCPU / 16 GiB，并将提交
评测和 AI 任务迁移到独立 worker。

## 当前服务器边界

| 资源 | 当前规格 | 影响 |
| --- | ---: | --- |
| CPU | 2 vCPU | 页面请求足够，多个 C++ 编译或 Python 统计会竞争 CPU |
| 内存 | 2 GiB | 可运行轻量 Flask + Redis 客户端；不适合在 Web 进程内加载训练模型或堆积任务 |
| 公网带宽 | 3 Mbps | 理论上约 375 KB/s；大 HTML、Monaco、图表和多个用户下载会很快占满 |
| 磁盘 | 40 GiB ESSD Entry | 小规模代码和日志足够；提交历史、上传文件、日志轮转需要持续监控 |
| 数据库 | 当前由 `DATABASE_URL` 决定 | 连接数和磁盘延迟会直接决定列表、统计和提交接口的并发上限 |

3 Mbps 是整台实例的出口上限，不是每个用户的上限。启用 gzip、静态资源缓存
和 Nginx/CDN 能降低传输量，但不能突破带宽总上限。

## 已落地的性能保护

- 生产 WSGI 默认使用 `gthread`，默认 2 个 worker、每个 4 个线程；避免同步
  worker 被长时间等待的 AI/SSE 请求完全占满。
- 数据库按每个 worker 设置有限连接池：默认 `DB_POOL_SIZE=3`、
  `DB_MAX_OVERFLOW=2`。默认 2 worker 时理论连接上限约为
  `2 × (3 + 2) = 10`，实际还要给迁移、管理工具和数据库自身预留空间。
- MySQL 连接开启 `pool_pre_ping`、回收周期和获取超时；SQLite 使用 busy timeout
  和独立线程连接参数。
- 高频查询增加组合索引，首页不再加载全部历史代码正文；班级统计、作业进度、
  学生作业列表和能力班级平均值减少 N+1 查询。
- AI 统一使用有限重试、熔断、provider 切换、并发上限和 Redis/本地缓存；同一
  进程中完全相同的未完成请求会合并为一次 provider 调用。缓存是精确请求缓存，
  没有使用模糊相似度缓存，以免把不同学习上下文错误复用。
- 能力分析和引导预设增加进程内任务去重；生产默认不在每个 Web worker 启动时
  建表、执行历史迁移或扫描全部预设。
- `/healthz` 是不访问数据库的轻量存活探针，`/readyz` 检查数据库连接，便于
  反向代理和进程管理器摘除异常 worker。
- 生产 HTML/JSON 可按配置 gzip，静态文件可配置浏览器缓存；SSE 和其它流式
  响应不会被压缩逻辑读取或阻塞。

## 容量估算（需要压测校准）

下表以 2 worker、每个 4 线程、单节点 Redis、MySQL 在本机或低延迟网络、AI
供应商配额正常为前提。它是容量规划的起点，不是 SLA。

| 场景 | 当前规格的建议上限 | 主要先到达的瓶颈 |
| --- | --- | --- |
| 健康检查/小型 JSON 接口 | 约 20–50 req/s 的规划区间 | Python worker、数据库往返；需用真实接口压测确认 |
| 普通页面浏览 | 约 10–20 名活跃用户 | 3 Mbps 出口、模板渲染和列表查询 |
| AI 对话/代码建议 | 同时约 4–6 个 AI 请求 | 每进程 AI 并发上限合计、供应商延迟和配额 |
| 集中代码提交/编译 | 建议先按 1–2 个并发编译规划 | 2 vCPU CPU 争用、编译进程和超时 |
| 任务堆积 | 队列应保持短于 20–30 个待处理任务 | 进程内队列非持久化，多 worker 之间不共享 |

AI 的“并发请求数”不等于在线人数：一个用户可能在等待 AI 时占用一个
provider 请求，但浏览页面不会占用 AI 槽位。AI 响应延迟若从 5 秒变成 20 秒，
同样的在线人数也会显著增加排队时间。

## 本机冒烟验证

2026-09-02 在开发机使用 `student-eval` 环境、`app.py`、本地 Redis 和当前
MySQL 配置做了轻量验证。最新一次应用重启后 `/healthz` 和 `/readyz` 均返回
200；20 次 `/healthz` 请求的观测值为 p50 约 2.53 ms、p95 约 3.97 ms、
最大约 219.68 ms；10 次 `/login` 请求的 p50 约 4.29 ms、p95/最大约
29.23 ms。

这些数字只反映本机环回、低负载、开发服务器的单机表现，不能代表阿里云
公网、Gunicorn、多用户或 AI 请求性能；正式容量仍需按照下一节进行逐级并发
压测。AI provider 的响应延迟也不包含在上述普通页面数字中。

代码验证方面，当前全量回归得到 601 项通过；另有 6 项与本次性能改造无关的
研究材料检查失败，原因是仓库缺少 `research/guided_learning_paper/README.md`
和 `literature_matrix.md`，以及已有的仿真冻结哈希不一致。AI、SSE、第三阶段
论坛和任务队列专项回归为 284 项通过，新增队列边界测试也通过。

## 升级建议

### 继续使用当前规格

适合开发、答辩演示、单个小班试用。建议保持：

- 2 个 Gunicorn worker、4 个线程；不要按“CPU×2+1”盲目开到 5 个 worker；
- `AI_MAX_CONCURRENT_REQUESTS=2` 或 `3`，避免 2 GiB 节点在 AI 和编译同时发生时
  被拖垮；
- 开启 Redis 会话/缓存，关闭生产启动期自动建表；
- 使用 Nginx 提供静态文件和 gzip，应用只处理动态请求；
- 配置日志轮转，并为上传、临时编译目录和磁盘空间设置告警。

### 推荐的第一档生产规格

适合一到几个班级、约 30–50 名活跃用户，且 AI 请求存在短时峰值：

- 应用节点：4 vCPU / 8 GiB；公网带宽至少 10 Mbps，建议 20 Mbps；
- 数据盘：80 GiB 起，并将日志、上传和编译临时文件分开监控；
- MySQL 和 Redis：至少保证低延迟、连接数配额足够，最好与 Web 节点分离；
- 评测和 AI：从 Web 进程内线程队列迁移到独立 worker，避免编译或 AI 阻塞页面。

### 面向更高并发

超过约 100 名活跃用户或需要多个班级同时上机时，建议 8 vCPU / 16 GiB 起步，
使用负载均衡和至少两个应用节点；数据库、Redis、C++ 沙箱和 AI 任务独立扩容。
此时不能继续依赖进程内任务队列，因为它不具备跨 worker 的持久化、重试和全局
去重能力，应接入 Celery/RQ/消息队列并设置任务幂等键。

## 监控阈值

连续 5 分钟满足以下任一条件，就应先限流或扩容，而不是继续增加 worker：

- CPU 使用率 > 70%；
- 内存使用率 > 75%，或开始使用 swap；
- 数据库连接池获取等待 > 1 秒，或连接池耗尽；
- 非 AI 请求 p95 > 500 ms；
- AI 等待队列 > 3 秒，或待处理任务持续增长；
- 5xx 比例 > 1%；
- 3 Mbps 出口持续 > 70%；
- 磁盘剩余空间 < 25%，或日志目录单日增长异常。

## 部署与压测检查

生产部署先执行一次数据库维护，再启动 WSGI：

```bash
python database_maintenance.py
gunicorn -c gunicorn_config.py wsgi:application
```

应用启动后先检查：

```bash
curl http://127.0.0.1:5000/healthz
curl http://127.0.0.1:5000/readyz
```

健康检查只能验证存活和数据库连接，不能替代压测。应在隔离环境用脱敏的真实
数据做以下三组测试：

1. 对 `/healthz` 和一个不触发 AI 的 JSON 页面做 60 秒逐级并发测试，记录 p50/p95、
   5xx、CPU、内存和带宽；
2. 用测试账号测试登录、作业列表、提交状态和阶段三恢复接口，验证数据库池和
   会话隔离；
3. 单独按 AI 供应商配额压测对话、代码建议和预设生成，记录首 token 延迟、完整
   响应延迟、限流次数、熔断次数和队列长度。不要用生产密钥做无上限压测。

压测结果应回填到本文件，重点替换上面的“规划区间”，再决定是否升级服务器。
